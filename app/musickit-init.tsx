'use client';

// This component exists only to ensure the MusicKit script tag is in the DOM.
// Actual configuration happens in the AppleMusicProvider via ensureMusicKit().
export default function MusicKitInit() {
  return null;
}
